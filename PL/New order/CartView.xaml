<Window x:Class="PL.New_order.CartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.New_order"
        mc:Ignorable="d"
        Title="CartView" Height="450" Width="800">
    <Grid>

        <ListView x:Name="Cart_list_view" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="0,0,0,42">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header = "ID" DisplayMemberBinding="{Binding Path = ProductID}"/>
                    <GridViewColumn Header= "Name" DisplayMemberBinding="{Binding Path = Name}"/>
                    <GridViewColumn Header= "Amount" DisplayMemberBinding="{Binding Path = Amount}"/>
                    <GridViewColumn Header= "Price" DisplayMemberBinding="{Binding Path = Price}"/>
                    <GridViewColumn Header= "Total Price" DisplayMemberBinding="{Binding Path = TotalPrice}"/>
                </GridView>

            </ListView.View>
        </ListView>
        <Grid Margin="0,392,0,0">
            <Button x:Name="confirmCartButton" Content="Confirm Cart" HorizontalAlignment="Left" Margin="681,0,0,0" VerticalAlignment="Center" Height="42" Width="119" Click="confirmCartButton_Click"/>
        </Grid>
    </Grid>
</Window>
